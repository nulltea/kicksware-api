project: kicksware
component: users
nameOverride: user-service
fullnameOverride: user-service
replicaCount: 1

image:
  repository: ci.kicksware.com:5050/root/kicksware-platform/users-service
  pullPolicy: IfNotPresent
  tag: latest

imageCredentials:
  secretName: registry-auth-secret
  registry: ci.kicksware.com:5050
  username: root
  token: R8QYV_Bqxii9qsPfoRT6

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  provider: traefik.containo.us/v1alpha1
  entrypoints:
    - web
    - websecure
  routes:
    - host: api.kicksware.com
      paths:
        - /users
        - /auth
        - /mail
        - /interact
      middlewares: []
      services:
        - name: user-service
          port: 8080
          scheme: http
  tls:
    enabled: true
    certResolver: le

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 25
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

serviceAccount:
  create: true
  annotations: {}
  name: ""

config:
  environment: PROD
  configMountPath: /dist/env

environment:
  name: PROD
  configPath: /env/config.yaml